# v2.5 架构改进完成 + 数据库同步

## 变更原因
执行 v2.5 架构改进的 4 项关键优化，并同步数据库 schema。

## 修改点

### 1. ChapterOutline 增加 7 个新字段
- `chapterType`: 章节类型（8 种类型）
- `readerPromise`: 读者承诺
- `chapterFunction`: 章节功能
- `requiredReaderEmotion`: 读者应感受到的情绪
- `payoffChainRefs`: 关联的爽点链阶段
- `comedyMechanism`: 喜剧机制
- `endingQuestion`: 章末悬念问题

### 2. Beat 蓝图混合模式
- 普通章节：使用程序模板生成 Beat（8 种 chapterType 对应 8 套模板）
- 关键章节（payoff/danger_escalation/info_reveal + 高潮章/转折章）：使用 LLM 生成

### 3. P3 层增加"本书固定口味"
StyleDna 新增：
- `fixedTaste`: 读者主要来看什么、喜剧来源、核心矛盾/反差、标志性桥段
- `chapterRhythm`: 每 N 章的节奏规则

### 4. 数据库同步
- 修复 prisma push 错误（数据库文件锁定问题）
- 成功迁移旧数据到新 schema
- 所有 150 章、109 个章纲、6 个角色数据完整保留

## 文件列表

| 文件 | 变更说明 |
|------|----------|
| server/prisma/schema.prisma | ChapterOutline 增加 7 个新字段，StyleProfile 增加 styleDna 字段 |
| server/prisma.config.ts | 修复 ESM 兼容性问题（使用 __dirname 替代 import.meta.url） |
| server/src/services/pipeline/generators.ts | 章纲输出增加新字段 + chapterType 说明 |
| server/src/services/pipeline/pipelineUtils.ts | 持久化新字段 |
| server/src/services/pipeline/writingPhase.ts | Beat 蓝图混合模式（模板+LLM） |
| server/src/services/pipeline/prompts/layer2-style.ts | P3 增加固定口味和章节节奏规则 |
| server/src/routes/workspace.ts | 工作台 API 返回新字段 |

## 风险说明
- 数据库已成功同步，所有历史数据完整保留
- Beat 模板需要在实际使用中验证效果
- 固定口味字段需要在风格生成时自动填充

## 验证情况
- [x] pnpm typecheck:server 通过
- [x] prisma push 成功，数据库已同步
- [x] 数据迁移完成（1 篇小说、150 章、109 个章纲、6 个角色）
- [ ] 需跑流水线验证完整流程
