# 变更记录 202605271600

## 变更原因
RAG Phase 2 Task 2：实现嵌入服务（EmbeddingService），为知识资产提供文本切片和向量嵌入能力。

## 修改点
- 新建 `EmbeddingService`，实现文本切片（CJK 感知分隔符）和 OpenAI 兼容嵌入 API 调用
- 三级配置降级：AppSetting(rag.embedding.*) > AIConfig(默认) > 环境变量
- JSON 内容自动扁平化为可读文本
- 批量嵌入处理（batchSize: 64），保证输出顺序与输入一致
- ENABLE_RAG feature flag 门控 + 单例模式

## 文件列表
| 文件 | 变更说明 |
|------|---------|
| `server/src/services/EmbeddingService.ts` | 新建：嵌入服务核心实现 |

## 风险说明
- 嵌入 API 调用失败时抛出异常，调用方需要做好错误处理
- 配置缓存在进程生命周期内有效，AppSetting 变更后需重启服务（或调用 clearConfigCache）
- ENABLE_RAG 未设置为 "true" 时 getEmbeddingService() 返回 null

## 验证情况
- TypeScript 类型检查通过（pnpm typecheck:server）
- 代码自审：修复了 SEPARATORS 多字符匹配问题和 splitRecursive 无限递归风险
