# 变更记录

**变更时间**: 2026-05-13 18:21

## 变更原因
统一工作区古风表单样式，确保所有输入框大小一致、排版整齐。

## 修改点
1. 创建统一古风样式文件 `workspace-ancient.css`
2. NovelWorkspace 内容区应用 `workspace-ancient` class
3. WorldviewEditor 表单使用 `form-group` 结构和统一按钮样式
4. CharacterCard 表单使用 `form-group` 结构和统一按钮样式

## 文件列表
| 文件 | 变更说明 |
|------|----------|
| `client/src/styles/workspace-ancient.css` | 新建：定义古风输入框、标签、按钮的统一样式 |
| `client/src/pages/NovelWorkspace.tsx` | 导入 CSS 文件，内容区添加 `workspace-ancient` class |
| `client/src/components/WorldviewEditor.tsx` | 表单项包裹 `form-group`，按钮使用 `btn-primary`/`btn-secondary` |
| `client/src/components/CharacterCard.tsx` | 表单项包裹 `form-group`，按钮使用 `btn-primary`/`btn-secondary` |

## 风险说明
- 样式可能与现有组件样式冲突，需确认优先级
- 如有其他组件也需统一表单样式，后续可继续应用 `workspace-ancient` class

## 验证情况
- [x] `pnpm typecheck:client` 类型检查通过
