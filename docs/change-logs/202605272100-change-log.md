# 变更记录 2026-05-27 21:00

## 变更原因

实现阶段二 Task 5：PipelineService RAG 集成，替换现有关键词检索为 RAG 混合检索。

## 修改点

### 修改文件
- `server/src/services/PipelineService.ts` — 替换 8 处 buildContext 调用为 RAG retrieve

### 功能变更

1. **检索替换**
   - 移除 `knowledgeSearchService` 导入
   - 新增 `getRagRetrieveService` 导入
   - 8 处 `extractKeywords + buildContext` 双步调用替换为 `retrieve()` 单步调用
   - 使用可选链 `?.retrieve()` + 空字符串回退 `?? ""`，RAG 未启用时安全降级

2. **保留不变**
   - `buildWorkspaceAssetContext()` — 结构化数据直接读取
   - `buildBookAnalysisContext()` — 拆书分析上下文
   - `buildImitationPlanContext()` — 仿写计划上下文

## 文件列表

| 文件 | 操作 | 说明 |
|------|------|------|
| `server/src/services/PipelineService.ts` | 修改 | 替换 8 处关键词检索为 RAG 检索 |

## 风险说明

- RAG 未启用时回退为空字符串，与之前 buildContext 返回空字符串行为一致
- `topK=10` 显式指定，与之前行为匹配
- 结构化上下文（大纲/人物/世界观等）不受影响

## 验证情况

- TypeScript 类型检查通过
- `knowledgeSearchService` 引用已完全移除（0 处）
- 8 处 RAG retrieve 调用已确认
- 9 处结构化上下文方法保留
- 已提交到 `feat/lightweight-rebuild` 分支
